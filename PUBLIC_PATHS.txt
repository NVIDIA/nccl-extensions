# This config is downloaded and parsed by the GitHub-First filtering pipeline
# Filtering script: https://gitlab-master.nvidia.com/nccl/ci-infra/-/blob/main/github-first/scripts/filter_repo.sh?ref_type=heads

[include]
# Paths to include in the filtered repository
# The repo is filtered down to these paths first before continuing to exclusions

nccl_ep
nccl_m2n
python
third_party
.clang-format
.clang-format-ignore
.gitignore
.gitmodules
CONTRIBUTING.md
LICENSE.txt
README.md

[exclude]
# Any paths within the inclusions above that should be removed
# Can be glob patterns

.gitlab/
**/.gitlab-ci.yml
PUBLIC_PATHS.txt

# Internal NCCL EP PLC design documents are never part of the public export.
nccl_ep/docs/plc/**

# Internal NCCL M2N PLC design documents are never part of the public export.
nccl_m2n/docs/plc/**

# Private M2N CI unit-test harness (not part of the public GitHub export).
nccl_m2n/tests/ci/**

# Internal M2N source inventory.
nccl_m2n/docs/source_layout.md
