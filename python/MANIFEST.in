include README.md
include LICENSE.txt
recursive-include nccl *.py *.pxd *.pyx
recursive-include nccl/m2n/include *.h

# Source distributions are portable source only; wheels own staged native libraries.
global-exclude *.so

# Binding-generation tooling is internal; never ship it in an sdist.
prune build_assets
